apiVersion: v1
kind: ServiceAccount
metadata:
  name: orders-service-sa
  namespace: apps
# Workload Identity annotation (iam.gke.io/gcp-service-account) is applied via
# `kubectl annotate` in docs/IMPLEMENTATION.md Step 6 — see catalog-service's
# serviceaccount.yaml for why. orders-service-sa is bound (in Terraform) to
# roles/cloudsql.client + roles/secretmanager.secretAccessor — see docs/DESIGN.md §7.
