apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalog-service-sa
  namespace: apps
# Workload Identity annotation (iam.gke.io/gcp-service-account) is applied via
# `kubectl annotate` in docs/IMPLEMENTATION.md Step 6, not baked in here, since the
# GCP project ID isn't known to this static manifest. catalog-service-sa needs no
# extra GCP IAM roles — see docs/DESIGN.md §7.
